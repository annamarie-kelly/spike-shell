{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Spike window: core IPC/events plus the folder-picker dialog (replaces the old osascript /pick).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-request-user-attention",
    "dialog:default",
    "updater:default",
    "process:allow-restart"
  ]
}
